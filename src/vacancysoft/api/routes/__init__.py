"""Per-topic route modules for the Prospero API.

Each submodule exposes a `router: APIRouter` that is wired into the
FastAPI app by `api/server.py`. See `docs/refactoring/week4-server-split.md`
for the split plan.
"""
